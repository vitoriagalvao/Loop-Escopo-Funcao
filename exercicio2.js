//2 - crie um algoritmo que converte dias em horas, quando recebe um número de dias.

function diasEmHoras(dias) {
  let hora = 24;
  const resultado = dias * hora;
  return resultado;
}

console.log(diasEmHoras(3));
